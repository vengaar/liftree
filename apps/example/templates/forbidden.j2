{% extends "layout.j2" %}
{% block body %}

<div class="ui error message">
  <div class="header">
    This path <i>{{ meta.path }}</i> is unavailable
  </div>
  <p>
    A path can be unavailable for many reasons
  <ul class="list">
    <li>This path doesn't exit.</li>
    <li>This path is not in a folders defined in the liftree configuration.</li>
    <li>This path is well inside a folder defined but in the excludes list.</li>
    <li>This path is marked as forbidden in mapping of liftree configuration.</li>
  </ul>
  </p>
</div>

<h2 class="ui header">Folders</h2>
<pre><code>{{ meta.config.folders|to_yaml }}</code></pre>

<h2 class="ui header">Mappings</h2>
<pre><code>{{ meta.config.mappings|to_yaml }}</code></pre>

{% endblock %}