---
- hosts: localhost
  gather_facts: true
  pre_tasks:
    - name: Ensure pre-requiste
      assert:
        that:
          - os_version in ansible_distribution in required.os
  roles:
    - role: setup
      git_clone: false
      wsgi_user: "{{ ansible_user_id }}"
...